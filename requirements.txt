mercapi==0.4.2
httpx==0.27.2
PyYAML==6.0.3

# 봇 명령으로 config.yaml 을 고쳐 쓸 때 주석과 서식을 보존한다.
ruamel.yaml==0.19.1

# 디스코드 봇. 끊겼을 때 다시 붙는 일과 슬래시 명령 등록을 대신해 준다.
# 웹훅만 쓴다면 없어도 되지만, 배포판은 어느 쪽이든 돌아야 해서 항상 넣는다.
discord.py==2.7.1

# 트레이 아이콘. 화면 없는 서버에서는 없어도 되며, 없으면 자동으로 비활성화된다.
pystray==0.19.5
Pillow==12.3.0
